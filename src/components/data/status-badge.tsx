import { Badge } from "@/components/ui/badge";
export function StatusBadge({ value }: { value: string | boolean }) { const label = typeof value === "boolean" ? (value ? "Active" : "Inactive") : value.replaceAll("_", " "); const normalized = String(value).toLowerCase(); const variant = normalized.includes("low") || normalized.includes("rejected") || normalized === "false" ? "destructive" : normalized.includes("draft") || normalized.includes("pending") ? "warning" : normalized.includes("active") || normalized.includes("approved") || normalized.includes("received") || normalized === "true" ? "success" : "secondary"; return <Badge variant={variant}>{label}</Badge>; }
