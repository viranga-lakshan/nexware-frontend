"use client";
import { AppShell } from "@/components/layout/app-shell"; import { PageHeader } from "@/components/layout/page-header"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Select } from "@/components/ui/select"; import { env } from "@/lib/env"; import { getWebSocketUrl } from "@/lib/realtime-config"; import { useUiStore } from "@/store/ui-store"; import { useWarehouses } from "@/features/warehouse/hooks/use-warehouses";
export function SettingsView(){const warehouses=useWarehouses();const{selectedWarehouseId,setSelectedWarehouseId}=useUiStore();return <AppShell><PageHeader title="Settings" description="Client-side preferences, warehouse context, and deployment environment details."/><div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle>Warehouse Context</CardTitle></CardHeader><CardContent className="space-y-3"><Select value={selectedWarehouseId??''} onChange={(e)=>setSelectedWarehouseId(e.target.value||undefined)}><option value="">All warehouses</option>{(warehouses.data?.content??[]).map(w=><option key={w.id} value={w.id}>{w.code} - {w.name}</option>)}</Select><p className="text-sm text-muted-foreground">Stored in Zustand as UI state only. API records stay in TanStack Query.</p></CardContent></Card><Card><CardHeader><CardTitle>Environment</CardTitle></CardHeader><CardContent className="space-y-2 text-sm"><p><span className="font-medium">API:</span> {env.apiBaseUrl}</p><p><span className="font-medium">WebSocket:</span> {getWebSocketUrl()||'Not configured'}</p><p><span className="font-medium">Architecture:</span> Next.js App Router, TanStack Query, Zustand, Axios.</p></CardContent></Card></div></AppShell>}
