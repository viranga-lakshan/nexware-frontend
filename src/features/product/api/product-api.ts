import { apiClient, unwrap } from "@/services/api-client"; import type { PageResponse } from "@/types/api"; import type { Product, ProductCategory } from "@/types/domain";
export const productApi={list:(params?:Record<string,unknown>)=>unwrap<PageResponse<Product>>(apiClient.get('/products',{params})),categories:()=>unwrap<ProductCategory[]>(apiClient.get('/products/categories')),create:(payload:unknown)=>unwrap<Product>(apiClient.post('/products',payload)),update:(id:string,payload:unknown)=>unwrap<Product>(apiClient.put(`/products/${id}`,payload)),remove:(id:string)=>unwrap<void>(apiClient.delete(`/products/${id}`))};
