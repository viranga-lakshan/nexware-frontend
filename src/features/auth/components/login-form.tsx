"use client";
import Link from "next/link"; import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import type { z } from "zod"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { loginSchema } from "../schemas/auth-schemas"; import { useLoginMutation } from "../hooks/use-auth-mutations";
type Values=z.infer<typeof loginSchema>;
export function LoginForm(){const login=useLoginMutation();const form=useForm<Values>({resolver:zodResolver(loginSchema),defaultValues:{email:'admin@nexware.lk',password:'Password123!'}});return <form className="space-y-4" onSubmit={form.handleSubmit(v=>login.mutate(v))}><div className="space-y-2"><Label>Email</Label><Input type="email" {...form.register('email')}/>{form.formState.errors.email&&<p className="text-sm text-destructive">{form.formState.errors.email.message}</p>}</div><div className="space-y-2"><Label>Password</Label><Input type="password" {...form.register('password')}/>{form.formState.errors.password&&<p className="text-sm text-destructive">{form.formState.errors.password.message}</p>}</div><Button className="w-full" disabled={login.isPending}>{login.isPending?'Signing in...':'Sign in'}</Button><div className="flex justify-between text-sm"><Link className="text-primary" href="/forgot-password">Forgot password?</Link><Link className="text-primary" href="/register">Create account</Link></div></form>}
